# GAMZAISSAC 개발 현황

이 문서는 다른 컴퓨터나 새로운 AI 대화에서 작업을 이어갈 때 현재 상태를 빠르게 파악하기 위한 개발용 인수인계 문서입니다. 일반적인 실행 방법과 플레이 조작은 [`README.md`](README.md), 외부 에셋 출처는 [`ASSET_CREDITS.md`](ASSET_CREDITS.md)를 우선 확인합니다.

마지막 코드 대조일: 2026-08-10

## 현재 개발 단계

- Phase 0~1.5 (기준선·플레이 루프·픽셀 아트): 완료
- 콘텐츠 확장 (아이템 33종·적 행동 5종·전투방 12종): 완료
- 모바일 터치 조작: 완료 (iOS 홈 화면 앱 재확인만 남음)
- 현재 위치: **알파 피드백 "재미없다" 대응 중 — 아이템 방향 전환 진행.** 방 반복
  (템플릿 12종)과 획득 체감(씨앗 크기·흡수 연출·HUD 강조)은 완료. 2026-08-16
  사용자가 방향 확정: 스탯 증가 위주에서 **자연스러운 시너지** 위주로 — 고정
  조합표를 늘리는 대신, 아이템들이 같은 공격 프로필을 고쳐 조합이 저절로
  늘어나는 아이작 방식을 지향한다. 1단계(시너지 발동 가시화) 구현 완료,
  2단계(쌓이는 거동 아이템: 뒷씨앗·물결·3점사)가 다음이다. 그 뒤 v1.0 잔여
  4항목(엔딩·밸런스·아트·QA)으로 돌아간다

## 협업 방식 (2026-08-10 갱신 — 반드시 읽을 것)

- **사용자**: 방향 결정, 직접 플레이 테스트, "재미있는가" 판단. 코딩 초보라
  설명은 쉽게 하되 과한 비유와 원칙 번호 태그는 쓰지 않는다.
- **Claude**: 주 개발자. 구현·버그 수정·자동 테스트·`npm run check`.
- **Codex(ChatGPT) 검수는 2026-08-10 종료** (요금제 만료). 검수는 이렇게 대체한다:
  - 위험한 변경(새 시스템·상태 기계·물리·여러 시스템 배선) → 구현 후
    `/code-review` 스킬 또는 사용자가 띄우는 별도 Claude 세션에 diff 검수.
    첫 실전에서 /code-review가 실제 결함(스폰-상자 겹침)을 잡아 검증됐다.
  - 수치 튜닝·색·단일 상수·데이터 추가 → 검수 생략.
  - Codex 시절 기준(회귀 위주, 기존 보스 동작 불변, 순수 함수 분리)은 계승한다.

**사용자가 당부한 것 (위반 금지)**

1. **명시적 요청 없이 커밋·푸시하지 않는다.** 구현이 끝나면 `npm run check` 통과를
   확인하고 커밋 없이 멈춰서 보고한다. 커밋 메시지는 요청 시 작성해 준다.
2. **커밋되지 않은 변경을 임의로 삭제·되돌리지 않는다.** 사용자의 작업일 수 있다.
   되돌리기도 사용자가 직접 실행하거나 명시적으로 승인한 경우에만 한다.
3. **습관적으로 동의하지 않는다.** 결론 → 이유 → 선택지 → 사용자가 정할 것 순서로
   답하고, 사실·기술 판단·취향을 구분한다. 사용자가 원하는 방향이라도 안전·버그·
   유지보수 문제는 미리 경고한다.
4. **구현 절차**: 기존 코드 확인 → 충돌·영향 설명 → 최소 범위 구현 → 검사 →
   커밋 없이 정지. 요청 범위를 임의로 넓히지 않는다.
5. **아이디어 스코프 4단계**: 기존 구조에 작게 얹으면 v1.0 / 새 대형 시스템은
   v1.1 / 기반 변경(조작·피해 모델)은 비용·위험 먼저 경고 / 애매하면 견적 먼저.
6. **v1.0 체크리스트에 없는 것은 하지 않는다.** 작업이 끝나면 "다음 뭐 할까요"
   대신 체크리스트의 다음 항목을 먼저 제시한다. 벗어나려면 근거를 대고 합의한다.
7. 이 게임은 첫 작품이자 다음 게임을 위한 발판이다. **아이작 모방은 의도된
   학습 방식**이므로 "독창성 부족"을 문제 삼지 않는다.
8. 검수·문서·커밋 메시지에 비밀번호 평문, 해시 생성 과정, 비밀 입력 순서를
   기록하지 않는다.

**알려진 함정**

- `npm run check`가 간헐적으로 전 테스트 파일을 `reading 'config'` 오류로
  실패시킨다. **재실행하면 통과한다.** 회귀로 오판하지 말 것.
- 모션·연출 작업은 Claude가 결과를 볼 수 없다. 레퍼런스(영상·이미지) 없이
  말로만 왕복하면 실패한다 — 퇴장 연출 5회 왕복 실패가 그 사례. 레퍼런스를
  받고, ffmpeg 프레임 추출로 분석한 뒤 구현한다.
- 픽셀 그림·아이콘은 구현 후 반드시 PNG로 렌더링해 눈으로 확인한다(스크래치
  패드에 렌더 스크립트 패턴 있음). 격자 문자열은 행 길이·좌우 대칭을
  프로그램으로 검사한다.

**아이템 추가 원칙 (2026-08-17 확정 — 자연 시너지가 저절로 성립하는 조건)**

- 새 거동은 반드시 공격 프로필(`PlayerAttackProfile`)의 속성으로 만든다.
  씬·시스템 코드에서 아이템/시너지 id로 분기하지 않는다 — 프리즘 배열이 처음에
  그렇게 만들어졌다가 검수에서 고쳐진 사례.
- 각 속성은 "이전 단계의 전체 결과"에 적용되게 설계한다 — 뒷발사는 앞 부채꼴
  전체의 거울, 점사는 볼리 전체의 반복, 물결은 모든 씨앗의 궤적. 이렇게 만들면
  새 속성 하나가 기존 전부와 자동으로 곱해진다.
- 플레이어 씨앗 발사는 `Player.spawnSeedFan` 한 경로만 쓴다. 별도의
  `Bullet.spawn` 지점을 만들면 그 거동부터 조합이 깨진다.
- 속성만 정의하고 효과가 없는 "이름뿐인 아이템"은 테스트가 잡는다:
  `attack-profile-consumption`(전투 코드가 속성을 읽는지)과 items 테스트
  (attackModifiers가 프로필을 실제로 바꾸는지).
- **단일 아이템 화력 상한 2배** (2026-08-17 밸런스 1차에서 확정): 어떤 아이템도
  혼자서 초당 화력을 2배 넘게 올리지 않는다. 특히 곱셈 배율(damageMultiplier)은
  다른 곱셈과 만나 폭발하므로 아껴 쓴다 — 메가씨드가 ×2로 단독 4.2배가 되어
  런 전체를 망가뜨렸던 사례.

## v1.0 완성 목표 (2026-07-24 확정)

감자이삭을 "완성된 게임"으로 마무리하는 것이 현재 최우선 목표다. 새 게임 구상은 `dev`
브랜치에 보존되어 있으며 v1.0 완성 후 재개한다. 완성은 "완벽"이 아니라 아래 목록이
비는 것을 뜻한다. **목록에 없는 것은 v1.0에서 하지 않는다.**

스토리 프레임: **감자의 농장 탈출기** — 땅속 깊은 곳에서 깨어난 감자가 위로 파고
올라가 농장을 탈출한다. 가볍고 유쾌한 톤.

- [x] 스테이지 구조 도입 (2026-07-25 완료): 4스테이지 × 2층 = 8층, 스테이지별 이름·색조,
      8층 탈출 엔딩(승리 화면)까지 구현. 1. 지하 깊은 곳(썩은 뿌리) 2. 어두운 흙
      속(지렁이 소굴) 3. 김 나는 퇴비더미 4. 빛이 새는 넝쿨 통로
- [x] 계층형 보스 구성 (2026-07-25 완료) — 모든 층 마지막에 보스방:
      I층 = 중간보스 4종(뿌리 옹이·꿈틀대는 덩어리·파리 여왕·가시넝쿨 뭉치,
      기존 적 AI 재사용·임시 외형), II층 = 스테이지 보스
- [x] 기존 보스 2종 표시명 재테마 (2026-07-25): ROOT KERNEL→썩은 뿌리핵(1스테이지),
      Fault Warden→퇴비 파수꾼(3스테이지). 외형 교체는 placeholder 교체 작업에서 진행한다
- [x] 늙은 지렁이 왕 (2스테이지 II층 = 4층, 2026-07-25): 전용 AI 클래스.
      꿈틀 돌진·땅굴 잠수 후 재등장 충격 링·새끼 지렁이 소환·"허물 벗기" 2페이즈.
      전용 PixelSprite 임시 도트 + idle 꿈틀·파고들기/솟기 프레임 애니메이션 (추후 최종 아트로 교체)
- [x] 녹슨 쇠스랑의 농부 (4스테이지 II층 = 8층 최종 보스, 2026-07-26): 전용 AI 클래스.
      1페이즈 5패턴(삼지창 찌르기·갈퀴 스윕·씨앗 흩뿌리기·발 구르기·쇠스랑 근접 휘두르기),
      2페이즈 "광란"에서 건초 커튼·회전 낫 부메랑 추가. 근접·부메랑은 직접 피해라 공통
      피격 피드백에 연결. 전용 PixelSprite 임시 도트(32×32→3배=96×96, 추후 사용자 제작 PNG로 교체)
- [x] (목록 외 편입) 모바일 터치 조작 + iOS viewport 보정 (2026-07-31~08-03)
- [x] (목록 외 편입) 아이템 33종·도트 아이콘 32종 (2026-08-03)
- [x] (목록 외 편입) 적 행동 2종(플랭커·소환사) + 공통 소환 기반 + 콘솔 enemy 명령
      (2026-08-04~05)
- [x] (목록 외 편입) 오프닝 연출 확장 — 도장 충격·파편·감자 무늬 배경 (2026-08-07~09)
- [x] 전투방 템플릿 12종·층 게이팅·직전 중복 방지 (2026-08-10, 알파 피드백 대응)
- [ ] 인트로 3~4줄 + 정식 탈출 엔딩 연출·크레딧 화면
      (현재 구현된 승리 화면은 점수·시간 요약만 보여주는 임시판이며, 이를 확장한다.
      이전 시도가 `git stash`에 있었으나 오래되어 새로 만드는 것을 권장)
- [x] 게임오버 버튼 문구를 실제 동작(타이틀 복귀)에 맞게 수정 (2026-07-24)
- [ ] 밸런스 1차 조정 (8층 난이도 곡선, 사용자 직접 플레이 기준)
- [ ] 가장 눈에 띄는 placeholder 교체 (보스 도트 등 — 범위는 협의)
- [ ] 최종 QA: 전체 런 3회 무사 완주 확인 후 배포

v1.0 이후 로드맵 (v1.1+): 이 프로젝트는 콘텐츠 상한 없이 계속 확장한다 (2026-07-25
사용자 방침). 단 버전 단위로 완성해 출시한다 — v1.0은 탈출 스토리 완성판. 이후 후보:
해금 메타 시스템(클리어마다 콘텐츠 추가, "가뭄의 대지" 스테이지, 트랙터·스키드더 보스),
기록 저장, 런 저장/불러오기, 키 리바인딩, 외부 랭킹, 일본어, 액티브 아이템, 코드 분할
최적화. 이것들은 v1.0에는 넣지 않는다.

## 최근 완료된 작업

- 상인 폭파 코인 + 전투방 아이템 보상 축소(2026-08-17, 커밋 전, 수치·데이터
  위주라 검수 생략): ① 상점 주인을 폭탄으로 날리면 절반 확률로 5코인이
  떨어진다(`rollShopNpcBlastCoinDrop`, 확률은 `REWARD_DROP_TUNING`) — 상인은
  방당 한 명이라 반복 획득 불가. ② 전투방 클리어 아이템 보상은 **순수
  능력치형만** 나오고(`isStatOnlyItem` — 보스 보상과 같은 기준) 드랍 확률도
  18%→7%(행운 상한 35%→14%)로 하향. 거동 아이템(쿼드샷·물결 등)은 보물방·
  상점에서만 나와 "기대하고 여는" 자리의 몫이 됐다
- 챔피언 적(2026-08-17, /code-review 검수 반영, 커밋 전): 재미 요소 1탄 —
  2층부터 전투방의 22% 확률로 자연 스폰 중 하나가 금색 챔피언(체력 ×2.2·점수
  ×3·크기 ×1.22)으로 승격되고, 잡으면 죽은 자리에 **보물 상자**를 확정으로
  떨군다. "저놈부터 잡을까"라는 전투 중 판단을 만드는 것이 목적. 방당 최대
  1마리, **방당 1회만 추첨**(`RoomNode.championRolled` — 재진입 재추첨으로 상자
  반복 획득 불가), 보스방은 추첨하지 않음. 분열 새끼·소환 하수인·콘솔 스폰은
  승격 경로 밖이라 자연 제외. 검수가 잡은 결함 반영: 사수형 챔피언이 첫 발사에
  금색을 잃던 버그(clearTint → restorePersistentTint), 승격 후 몸 반경이 낡아
  벽에 파고들던 버그(effectiveBodyRadius를 현재 배율 getter로), 런 종료
  프레임의 상자 생성 가드, 벽에 붙어 죽으면 상자가 방 밖으로 밀려나던 문제
  (생성 위치 클램프), 챔피언 식별을 튜닝 수치가 아닌 명시적 플래그로 분리,
  엔티티가 스폰 정책 상수를 직접 읽던 계층 문제(승격 수치를 파라미터로).
  상자는 상자 코인 드랍과 같은 지속 경로(`spawnPersistentReward`)라 방을
  나갔다 와도 남는다
- 밸런스 1차 조정 1회차(2026-08-17, 커밋 전, 수치 튜닝이라 검수 생략): 사용자
  플레이 피드백 3건(체력템 과다·보스 물몸·메가씨드 원맨쇼)을 실측으로 확인 후
  조정. ① 드랍 20만 회 실측에서 체력템이 전투 13.3%·보물 19.2%·보스 38%로
  나와, bark-vest·pocket-battery를 상점 전용으로, silver-dew를 보물 풀에서
  제외 → 전투 6.6%·보물 10.5%로 절반 감소. **보스 보상 38%는 사용자 지시로
  유지**(랜덤의 맛), 상점도 선택형이라 유지. ② 메가씨드 +4/×2(단독 화력
  4.2배) → +2.5/×1.3(1.9배) — "단일 아이템 화력 상한 2배" 규칙 신설(아이템
  추가 원칙 참조). ③ 보스 체력을 "그 층 평균 빌드로 미드 약 14초, 스테이지
  22-38초" 목표로 역산해 상향: 중간보스 14/12/13/13 → 46/50/56/60, 스테이지
  보스 뿌리핵 30→80·지렁이왕 22→100·파수꾼 26→125·농부 34→165. 기존에는
  중반 빌드로 미드보스가 2-4초에 죽어 2페이즈 패턴을 볼 시간이 없었다.
  **역산에 쓴 예상 화력 곡선은 추정이므로 사용자 플레이로 1~2회 재조정 전제**
- 쌓이는 거동 아이템 3종(2026-08-16~17, /code-review 검수 반영, d121f66): 아이템 34번부터 36번 —
  **뒷주머니 씨앗**(앞 부채꼴 전체를 뒤로 거울 복사 — "뒤로 몇 발"이 아니라
  거울 규칙이며, 쿼드샷을 먹으면 뒤도 4갈래. 플레이 피드백으로 확정),
  **물결 씨앗**(궤적이 물결침, 스탯 무변경 첫 순수 거동 아이템),
  **삼연발 꼬투리**(3발 점사 후 쉼, 평균 연사 유지). 셋 다 공격 프로필의 서로
  다른 축(`rearFire`·`waveDegrees`·`burstCount`)이라 기존·신규 아이템과 조합이
  저절로 성립한다 — 물결+쿼드샷 = 엇갈려 물결치는 4발, 뒷주머니+쿼드샷 = 앞뒤
  4갈래. 물결은 위치가 아니라 속도
  방향만 조향해(`SeedWaveRules` 순수 함수, 단위 테스트) 벽 충돌·수명 물리가
  그대로다. 검수가 잡은 결함 반영: 점사 쿨다운은 선불이 아니라 **쏜 발마다
  쌓아** 끊어 쏘는 플레이가 손해 보지 않고, 점사 간격은 기본 발사 간격보다 길 수
  없으며(고연사 빌드 보호), 빔 전환 시 점사 잔여분을 비우고, 물결 조향은 각도
  오프셋 방식으로 바꿔 프레임당 atan2 왕복 없이 속도 크기를 보존하며(외부 탄속
  효과와 공존), 앞뒤 부채꼴의 물결 위상을 이어 센다. 도트 아이콘 3종은 PNG
  렌더로 눈 확인 완료. 보스방 보상 풀에서는 attackModifiers 규칙으로 자동
  제외되며 테스트로 고정. **알려진 한계(의도)**: 물결은 실질 도달 거리가 약 9%
  짧아지는 트레이드오프가 있고, 차징 빔 빌드에서는 거동 3종이 무효라 스탯
  페널티만 받는 안티시너지가 존재한다(아이작류 관례로 v1.0 허용)
- 시너지 발동 가시화 + 프리즘 배열 실효과(2026-08-16, /code-review 검수, cd5f836):
  실측 결과 시너지 6종은 발동해도 **화면에 아무 안내가 없었고**(발동 결과를 아무도
  소비하지 않음), 프리즘 배열은 효과 정의가 빈 이름뿐인 시너지였다. 발동하면
  시너지마다 스크롤 알림("{이름} 시너지 발동!" + 효과 설명)이 뜨고, 금백색 링과
  전용 효과음은 **알림이 실제 화면에 나타나는 순간**(`onShow` 콜백) 재생한다 —
  즉시 재생하면 줍기 연출과 겹쳐 묻히고 스크롤은 대기열 뒤라 몇 초 어긋나기
  때문. 배선은 `applyPassiveAcquisition` 한 곳이라 줍기·구매·미래의 획득 경로가
  시너지 안내를 빠뜨릴 수 없다. 프리즘 배열 실효과는 처음에 "빔과 같은 방향 씨앗
  동시 발사"로 넣었으나 사용자 플레이 판단(관통 빔과 겹쳐 낭비)으로 **빔 차징 30%
  단축**으로 교체했다 — 공격 프로필 배율 `beamChargeMsMultiplier`라(자연 시너지
  방향과 일치) 어떤 아이템·시너지든 줄일 수 있고, 쿼드샷의 연사 페널티로 느려진
  차징(약 2초)을 눈에 띄게 되돌린다. 씨앗 발사 몸통은 `Player.fireSeedVolley`
  하나로 합쳐 다음 발사 경로도 같은 부채꼴·수치 계산을 쓴다. 발동한 시너지는
  id가 아니라 정의 객체로 전달해 "알림 없이 연출만 나오는" 경우가 구조적으로
  불가능하다. 아이템 33종 실측 분류(시각/능력 7 : 순수 스탯 26)는 이 작업의
  근거였다
- 전투방 템플릿 12종 확장(2026-08-10, /code-review 검수): 한 판 44개 전투방 대비
  조합이 8개뿐이라 같은 방을 평균 5.5회 반복하던 것을 36조합·1.2회로 완화했다.
  `minFloor` 게이팅으로 층이 오를수록 새 방이 풀리고, 그 덕에 막혀 있던 플랭커
  (3층)·소환사(5층) 고정 배치가 가능해졌다. 선택은 `RoomTemplateRules`로 분리해
  직전 템플릿을 제외하고 뽑는다. 템플릿이 깐 적은 `getReinforcementIds`의
  `alreadyPresent`로 증원 상한에 반영해 방당 상한(플랭커 2·소환사 1)이 배치
  출처와 무관하게 지켜진다. /code-review가 스폰-상자 겹침 10곳을 잡아 수정했고,
  적 몸 반경 기준 간격 테스트로 재발을 막았다
- 상자 장애물 도트 교체와 충돌 범위 수정(2026-08-10): 혼자 매끄럽던 상자를
  20×20 격자 도트로 다시 그렸다. 정적 바디가 setScale을 따라가지 않아 그림보다
  25% 크던 원래 있던 버그를 함께 고쳤다 — 축소를 먼저 하고 바디를 만든 뒤
  실제 그려진 상자 크기(29×27)로 지정한다
- 오프닝 연출 확장(2026-08-07~09): 스킵 안내가 도장처럼 찍히며 화면이 흔들리고
  흙먼지·조약돌·뿌리·감자가 떨어진다. 배경에는 투명 격자로 채운 감자 무늬가
  오른쪽 위로 계속 흐르고, 감자(창)보다 격자가 2.5배 느리게 흘러 시차가 생긴다.
  퇴장 연출(체스판·3D 제목 등)은 5회 왕복 끝에 취소했다 — 레퍼런스 없이 말로만
  조율한 것이 실패 원인
- 소환형 적 부르는 자 추가(2026-08-05, Codex 2회 검수): 5층부터 등장하며 거리를 벌리다
  4200ms마다 하수인 둘을 부른다. 눈앞의 적부터 처리하는 습관을 무효화해 "뒤를 먼저
  칠까"라는 판단을 만드는 것이 목적이다. 예고 420ms 동안 완전히 멈추는데, 잡을 틈이
  없으면 쫓아가는 것 말고 대응할 방법이 없기 때문이다. `summon-request`는 예고가
  끝나는 순간에 쏘므로 예고 중 처치하면 소환 자체가 일어나지 않는다. 쿨다운은 예고
  시간을 빼고 계산해(`getNextTelegraphAt`) emit 사이 간격이 정확히 4200ms가 되게 했다.
  예고 링은 로컬 좌표로 그리고 매 프레임 위치만 맞춰, 피격 넉백에 본체만 밀려나도 링이
  따라온다. 이동 속도에는 층 배율을 곱하지 않는다 — 정의에 적힌 값과 실제 값이 달라지면
  수치를 믿을 수 없기 때문이며, 느리면 `enemies.ts`의 `speed`를 직접 올린다.
  방당 최대 1마리
- 적 소환 규칙을 공통 기반으로 분리(2026-08-05): 지렁이 왕 전용이던 소환 코드를 모든
  적이 쓸 수 있게 `EnemySummonRules`로 옮기고 `boss-summon` → `summon-request`로
  일반화했다. 기존에는 방 안 적 수에서 보스 자신만 빼는 계산이라 일반 전투방에서는
  원래 있던 적까지 하수인으로 세어 소환이 막혔다. `SummonOwnershipIndex`로 소환사별
  하수인을 추적해 각자 개인 상한을 따로 쓰게 했고, 하수인 쪽 역방향 색인으로 세므로
  소환사가 죽어도 남은 하수인이 방 전체 상한을 계속 차지한다. 방 전체 상한 4는 일반
  전투방에만 적용한다 — 지렁이 왕은 새끼 5마리를 유지하도록 맞춰져 있어 보스방에
  적용하면 조용히 약해진다. 소환 등록과 같은 블록에 있던 농부의 `player-damaged`
  피격 피드백은 보스 전용으로 그대로 남겼다
- 개발자 콘솔 `enemy <적 ID>` 명령 추가(2026-08-04): 현재 방에 적을 즉시 생성한다.
  이름 없이 입력하면 목록을 보여주고 적 ID 자동완성도 지원한다. 플랭커는 3층부터
  증원으로만 등장해 상자와 함께 나올 확률이 15%라 새 적 행동을 검증하기 어려웠다.
  생성은 `RoomController.spawnDeveloperEnemy`를 거쳐 기존 `registerSpawnedEnemy`
  경로를 그대로 타므로, 콘솔로 부른 적도 자연 배치된 적과 동일하게 동작한다
- 측면 포위형 적 플랭커 추가(2026-08-04, Codex 3회 검수): 직선으로 쫓지 않고 플레이어
  주위 링 위를 옆으로 파고들어 자리를 잡은 뒤에야 돌진한다. 뒤로 물러나는 것만으로는
  안전하지 않게 만드는 것이 목적이다. 상태는 circling→windup→lunging→recovering
  네 개이며 전부 `updateAI(time)` 시간 비교로 전환해 방 전환 후 타이머가 남지 않는다.
  좌표 계산은 `FlankerRules`로 분리했고, 벽에 clamp된 목표에서는 원래 각도가 아닌
  실제 목표점의 각도로 돌진을 판정해 구석에서 영구 선회하는 것을 막았다. 상자에 막히면
  400ms 정체를 감지해 반대쪽으로 목표를 다시 잡고 막힌 축과 수직으로 짧게 미끄러진다.
  3층부터 증원으로만 등장하며 방당 최대 2마리
- 아이템 아이콘 전면 도트 교체와 신규 패시브 8종(2026-08-03): 색만 입힌 배지 대신
  16×16 격자에 한 픽셀씩 찍은 아이콘 32종을 `itemPixelIcons`에 넣었다. 아이템은
  25종에서 33종이 되었고 절반이 트레이드오프형이다. 쿼드샷을 압도하지 않도록 쌍둥이
  씨앗은 중첩 불가로 두었다. 모든 아이템이 도트 아이콘이나 실제 PNG 중 하나를 갖도록
  테스트로 강제한다
- iOS Safari·홈 화면 실행 viewport 보정(2026-07-31): 게임 컨테이너를 실제 가시 높이인
  `dvh`에 맞추고 `viewport-fit=cover` 안전 영역을 적용했다. 모바일 스틱은 홈
  인디케이터에서 추가로 띄워 조작하기 편한 위치로 올렸다
- 모바일 터치 조작 2단계 구현(2026-07-31): 키보드 입력을 불리언 스냅샷으로 추상화한
  뒤, DOM 기반 이동 8방향·사격 4방향 멀티터치 스틱과 폭탄·구매·일시정지 버튼,
  실제 미니맵 터치 확대를 연결했다. 오프닝·일시정지·런 종료·씬 종료 때 입력을
  초기화하며 세로 화면에는 가로 회전 안내를 표시한다
- 스테이지 구조 공사 완료 (2026-07-25, 9단계 — Codex 설계 검토·단계별 검수 방식):
  방→층→스테이지 계층, 층별 보스 선택(`stages.ts` 데이터 기반), `RunState.outcome`
  단일 런 종료 상태, 최종층 탈출구와 탈출 성공 화면(게임오버와 완전 분리),
  스테이지별 방 색조와 층·스테이지 표시, 콘솔 `floor` 1~8 제한
- 중간보스 4종 추가(기존 적 AI·텍스처 재사용 + displayScale 확대), 사수 적이 공격 후
  축소된 채 고정되던 버그 수정
- 2층부터 등장하는 분열형 적 추가: 부모(`splitter`) 처치 시 새끼(`splitterling`) 둘로 갈라지며, 새끼는 다시 분열하지 않음
- 증원·분열 계산을 `EnemyReinforcementRules`로 분리하고 2층 등장 조건과 자식 생성 단위 테스트 추가
- 개발자 콘솔 `kill` 명령이 분열로 생성된 새끼까지 모두 처치하도록 수정
- 패시브 아이템을 25종으로 확장하고 아이템 번호, 희귀도, 분류, 최대 중첩과 방별 드랍 출처 추가
- 두 아이템 조합으로 한 번만 발동하는 시너지 6종 추가
- 전투방 아이템 드랍과 방 종류별 아이템 풀 개선
- 방 클리어 보상, 부순 장애물 보상과 미획득 아이템의 방 재입장 상태 유지
- 체력이 가득 찬 하트와 보상 상자 밀기, 열린 상자 상태 유지
- 아이템 획득 스크롤 알림과 연속 획득 대기열 추가
- 개발자 콘솔 로직을 `DeveloperConsoleController`로 분리
- 이미지·아이템 번호 기반 개발자 아이템 선택기 추가
- 개발자 콘솔과 아이템 선택기 창의 드래그·크기 조절·위치 저장 추가
- 인증 후 `F2`로 아이템 선택기 열기/닫기 추가

## 구현된 게임 흐름

1. 타이틀에서 게임을 시작합니다 (감자의 농장 탈출기 — 땅속에서 위로).
2. 시작방에서 전투방, 상점방과 보물방을 탐색합니다.
3. 전투방을 클리어하면 자원 보상 또는 아이템이 확률적으로 생성됩니다.
4. 보스방에서 층별 보스(I층 중간보스, II층 스테이지 보스)를 처치하면 순수 능력치형
   보상과 위로 뚫린 뿌리 굴이 생성됩니다.
5. 굴에 들어가면 층이 증가하고 던전이 새로 생성됩니다 (4스테이지 × 2층 = 총 8층).
6. 8층 보스를 처치하면 빛이 새어드는 탈출구가 생성되고, 진입하면 탈출 성공 화면
   (방·아이템·점수·시간)을 표시한 뒤 타이틀로 돌아갑니다.
7. 체력이 모두 소진되면 게임오버 결과를 표시한 뒤 타이틀로 돌아갑니다.

## 상태 유지 범위

### 현재 런 안에서 유지됨

- 현재 층, 점수, 클리어 방 수
- 체력과 능력치
- 열쇠, 폭탄과 코인
- 획득한 아이템, 해금 능력과 발동한 시너지
- 방별 클리어 여부와 특수방 잠금 해제 여부
- 방에 남겨둔 아이템과 보상의 종류·상태·위치
- 상점 상품, 구매 여부와 할인 상태
- 보스 보상 선택 및 획득 여부

### 브라우저를 닫거나 새로고침해도 유지됨

- 언어
- 사운드, 효과음 음량, 화면 흔들림과 렌더 품질 설정
- 개발자 콘솔과 아이템 선택기 창 위치·크기

### 아직 유지되지 않음

- 진행 중인 런 전체 저장/불러오기
- 최고 점수와 플레이 기록
- 게임 포럼 라운지 랭킹 등록

`runState.adminUsed`와 랭킹 등록 가능 여부 판단 함수만 준비되어 있습니다. 실제 외부 랭킹 API 연동은 아직 구현되지 않았습니다.

## 주요 안전 처리

- 방 입장 시 플레이어 보호 600ms
- 방 입장 시 적 AI 대기 400ms
- 적을 출입구와 장애물에서 떨어진 안전 위치에 배치
- 방 클리어 후 문 개방 500ms 지연
- 탄환 충돌 전 active/body 검사와 중복 소비 방지
- 게임오버·일시정지 나가기의 타이틀 전환에 브라우저 타이머 fallback 적용
- Phaser Scene 재사용 시 런 전용 상태를 명시적으로 초기화
- 오디오 호출 실패가 게임 루프를 중단하지 않도록 예외 처리

## 개발자 도구

- 게임 중 개발자 콘솔을 열 수 있습니다.
- 콘솔은 비밀번호 인증 후 명령을 실행합니다.
- 자동완성, 명령 기록, 마우스 이동과 크기 조절을 지원합니다.
- 인증 후 `F2`로 아이템 선택기를 열 수 있습니다.
- 아이템 선택기는 아이콘, `ID: 001` 형식의 번호와 이름을 표시합니다.
- 실제 게임 상태를 바꾸는 관리자 명령을 사용하면 화면에 `ADMIN`이 표시되고 해당 런은 랭킹 등록 불가 상태가 됩니다.

비밀번호 원문, 해시 생성 과정과 비밀 입력 순서는 이 문서에 기록하지 않습니다. 필요한 경우 현재 구현 코드를 직접 확인하되 공개 문서나 커밋 메시지에 비밀번호 원문을 남기지 않습니다.

## 에셋 적용 상태

### 적용 완료

- Snoblin Yellow 플레이어 스프라이트와 이동·피격·사망 애니메이션
- 직접 제작한 감자 상인 2프레임 애니메이션
- 직접 제작한 `red-mushroom` 아이콘
- 1-bit HUD 하트·열쇠·폭탄·코인·능력치 아이콘
- BoldPixels 영문·숫자 폰트와 DungGeunMo 한글 웹폰트
- 타이틀·일반방·상점·보스방 배경음악 4종
- 보물상자 스프라이트와 아이템 획득 스크롤 이미지

### placeholder 또는 추후 교체

- 일반 적과 보스 외형
- 방 바닥, 벽과 문 일부
- 전투방 나무 장애물 이미지
- 일부 아이템 아이콘과 전투 효과

사용자가 제공한 새 나무상자 이미지 교체 작업은 보류했습니다. 관련 이미지와 로딩 코드는 현재 커밋에 포함되어 있지 않으며, 추후 별도 작업으로 진행합니다.

## 보류되었거나 아직 없는 기능

- 액티브 아이템과 액티브 아이템 슬롯
- 전체 런 저장/불러오기
- 키 리바인딩
- 일본어 번역
- 외부 랭킹 시스템 연동
- 층별 신규 일반 적·방 구조·보스 추가
- 자판기 등 추가 상점 기믹
- 남은 placeholder 도트와 효과음 교체
- 프로덕션 번들의 코드 분할과 초기 다운로드 최적화

## 확인된 문구 불일치

- (해결됨 2026-07-24) 게임오버 버튼 문구를 실제 동작에 맞게 `타이틀로 돌아가기(Back to Title)`로 수정했습니다.

## 다음 작업 추천 순서

1. **테스터 답변 수집** — "재미없다"의 원인 확정. 물어볼 것: 몇 층에서 지루해졌나 /
   그만뒀나 계속했나 / 기억에 남는 아이템이 있었나 / 죽고 다시 하고 싶었나 /
   방마다 다르게 느껴졌나 / 조작이 답답했나. 특히 "다시 하고 싶었나"가 핵심
2. 답변에 따라: 아이템 체감(숫자만 오르는 문제) 또는 전투 손맛 개선
3. v1.0 체크리스트 잔여: 엔딩·크레딧 → 밸런스 1차 → placeholder 아트 → QA 3회·배포
4. 미해결 확인 2건 처리(아래 참조)

## 미해결 확인 사항 (플레이 확인 대기)

- **소환사 실기 확인 2건**: ① 예고 중 빔 연속 피격 시 호박색 링이 본체를
  따라오는가(`spawn prism-lance` 후 `enemy summoner`) ② 층 배율 제거 후 5층에서
  소환사를 추격할 수 있는가 — 느리면 `enemies.ts`의 `speed: 46`을 올린다
- **아이폰 홈 화면 앱**: 상단 여백 밀림 수정(`b70559c`) 후 재확인 안 됨.
  홈 화면 아이콘을 삭제하고 다시 추가해야 새 설정이 적용된다. 실패 시 예비책은
  `viewport-fit=cover` 제거
- **방 템플릿 12종**: 검사·검수는 통과했으나 사용자 플레이 확인 전이면 커밋
  전이다. corner-forts에서 적이 입장 순간 튀지 않는지 볼 것

## 주요 코드 위치

```text
src/config/   화면·전투·피드백·에셋 설정
src/data/     아이템·보상·적·방·상점 데이터
src/entities/ 플레이어·적·탄환·빔·문·픽업 오브젝트
src/scenes/   부팅·타이틀·게임·일시정지·타이틀 전환 Scene
src/systems/  던전·전투·아이템·보상·방 전환·오디오·개발자 도구 로직
src/ui/       HUD·미니맵·보스 HUD·설정·개발자 콘솔 UI
tests/        시스템별 단위 테스트
```

## 새 작업을 시작하기 전 확인 절차

1. `git status --short --branch`로 커밋되지 않은 변경을 확인합니다.
2. `README.md`와 이 문서를 읽습니다.
3. `git log --oneline -10`으로 문서 작성 이후의 최근 커밋을 확인합니다.
4. 요청한 기능과 관련된 실제 코드를 읽고 문서가 여전히 맞는지 검증합니다.
5. 작업 후 `npm run check`를 실행합니다.
6. 직접 플레이가 필요한 기능은 자동 검사와 별도로 브라우저에서 확인합니다.

문서보다 실제 코드와 Git 기록이 우선입니다. 기능을 추가하거나 동작을 바꾼 경우 같은 작업에서 README 또는 이 문서도 함께 갱신합니다.

## 현재 검사 기준

- ESLint
- Vitest 단위 테스트
- Prettier 형식 검사
- TypeScript 타입 검사
- Vite 프로덕션 빌드

`npm run check`에서 표시되는 500kB 이상 번들 경고는 현재 빌드를 실패시키지 않는 비차단 경고입니다. 기능 확장이 더 진행된 뒤 코드 분할과 초기 다운로드 최적화 작업에서 처리합니다.
